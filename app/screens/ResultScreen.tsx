import { View } from 'react-native';
import type { RootStackScreenProps } from '../../types/navigation';

export default function ResultScreen(_props: RootStackScreenProps<'Result'>) {
  return <View style={{ flex: 1 }} />;
}
