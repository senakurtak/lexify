import { View } from 'react-native';
import type { RootStackScreenProps } from '../../types/navigation';

export default function HowToPlayScreen(_props: RootStackScreenProps<'HowToPlay'>) {
  return <View style={{ flex: 1 }} />;
}
