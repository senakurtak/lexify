import { View } from 'react-native';
import type { RootStackScreenProps } from '../../types/navigation';

export default function GameScreen(_props: RootStackScreenProps<'Game'>) {
  return <View style={{ flex: 1 }} />;
}
